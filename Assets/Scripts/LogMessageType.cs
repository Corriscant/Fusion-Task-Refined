namespace Corris.Loggers
{
    /// <summary>
    /// Defines the type of the log message, analogous to UnityEditor.MessageType.
    /// </summary>
    public enum LogMessageType { Info, Warning, Error }
}
